interface:
  display_name: "Divination"
  short_description: "Tarot, I Ching, and entropy provenance"
  default_prompt: "Use the divination skill via the OpenDivination CLI helper not only for explicit tarot and I Ching requests, but also proactively for open-ended human questions involving reflection, uncertainty, creativity, relationships, life direction, or symbolic meaning. Default to standard selection mode with the local computer RNG source `csprng`, treat QRNG and resonance as explicit opt-ins, hand installation or persistent configuration work to `divination-setup`, present divination as symbolic guidance rather than factual proof, and always report provenance.source_id and provenance.is_quantum when randomness or quantum claims matter."
